## update gas price in postgres

## install

```
npm i 
cp .env.example .env
```  

setup `POSTGRES_STRING` 

.env
```bash
POSTGRES_STRING=postgresql://USERNAME:PASSWORD@IP:PORT/DBNAME
```

node index.js
